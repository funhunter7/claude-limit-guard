{
  "name": "claude-limit-guard",
  "version": "0.10.2",
  "description": "Claude Code plugin that watches subscription usage limits and gracefully saves/resumes work at a threshold.",
  "type": "module",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "node --test",
    "lint": "eslint .",
    "check": "npm run lint && npm test"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.4.1",
    "globals": "^17.6.0"
  }
}
